// Helper check script untuk memverifikasi nama guru di navbar
require('dotenv').config();
const http = require('http');
const url = require('url');

console.log('Script ini hanya referensi; jalankan server dan cek browser untuk nama guru di navbar.');
console.log('Tidak ada perubahan otomatis pada file publik.');
process.exit(0);
